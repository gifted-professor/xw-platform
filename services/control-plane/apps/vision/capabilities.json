{
  "schemaVersion": 1,
  "capabilities": [
    {
      "schemaVersion": 1,
      "id": "vision.resolve_tap_dry_run",
      "appId": "vision",
      "packageName": null,
      "versionRange": "observed",
      "maturity": "E2",
      "risk": "R0",
      "resources": [
        "device"
      ],
      "inputSchema": {
        "type": "object",
        "properties": {
          "label": {
            "type": "string",
            "minLength": 1,
            "maxLength": 40
          },
          "region": {
            "type": "string",
            "enum": [
              "tabbar",
              "topbar",
              "dialog",
              "chips",
              "header",
              "content"
            ]
          },
          "requireSafeNav": {
            "type": "boolean"
          },
          "executeTap": {
            "type": "boolean"
          },
          "elements": {
            "type": "array",
            "description": "可选：预分析元素表；缺省则从设备 dump 语义节点"
          },
          "resolution": {
            "type": "array",
            "items": {
              "type": "integer"
            },
            "minItems": 2,
            "maxItems": 2
          }
        },
        "required": [
          "label"
        ],
        "additionalProperties": false
      },
      "outputSchema": {
        "type": "object"
      },
      "preconditions": [
        "L1 安全闸：非空 label、禁止外发词、region、页面指纹（tabbar）",
        "executeTap 默认 false；为 true 时仍禁发布/发送/支付"
      ],
      "verification": {
        "mode": "state",
        "description": "resolve ok 或明确 no_match/forbidden；若 executeTap 则 stoppedBeforeOutbound"
      },
      "restoration": {
        "required": false,
        "description": "dry-run 默认不改状态；executeTap 仅导航类"
      },
      "timeoutMs": 60000,
      "idempotency": "replay_safe",
      "automationPolicy": {
        "mode": "lab_only"
      },
      "implementation": {
        "adapter": "vision",
        "action": "resolve-tap-dry-run"
      },
      "evidence": [
        "visual-grounding-poc L1-FINDINGS 02机",
        "label_matches empty-string footgun",
        "2026-07-26 multi-device publish concurrent"
      ],
      "availability": "implemented",
      "effect": {
        "class": "none",
        "phase": "na",
        "commitBoundary": "automatic"
      },
      "exposure": "public",
      "invocationPolicy": {
        "allowedModes": [
          "job",
          "session"
        ]
      },
      "lifecycle": "implemented"
    }
  ]
}
