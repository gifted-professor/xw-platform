# 运行产物不进 git（源码目录 = 部署源，运行态一律隔离）
*.log
*.png
*.bak*
registry.db
registry.db-journal
registry.db-wal
registry.db-shm
runtime/
node_modules/

# feishu-to-xianyu 下载的 Yupoo 原图运行态
tmp-imgs/

# campaign 运行态（step.sh/arm-driver.sh/fixtures/ARM-PROTOCOL 是源，logs 与 done/abort 标记是运行产物）
campaign/logs/

# watchdog 运行态（脚本与模板保留，state 与报告是运行产物）
watchdog/state.json
watchdog/reports/
watchdog/launchd.out.log
watchdog/launchd.err.log

# 工具生成的 wiki 缓存
.qoder/

# macOS 元数据
.DS_Store

# IDE / agent tooling（skill symlink，非源码）
.claude/*
.codex/
# 例外：本仓库自带的 project skill（registry-review）跟 git 走，换 Mac/重 clone 自动发现；
# 其余 .claude/ 仍 ignore（保护 aionui symlinks / 本地 settings）
!.claude/skills/
.claude/skills/*
!.claude/skills/registry-review/

# git worktree 存储（EnterWorktree 生成）
.gitworktrees/

# 本地数据库（非源码）
aionui-backend.db
