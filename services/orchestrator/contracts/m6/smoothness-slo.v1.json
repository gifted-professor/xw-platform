{
  "schemaId": "xw.smoothness-slo.v1",
  "schemaVersion": 1,
  "generatedAt": "2026-08-21T00:00:00Z",
  "freezeNote": "本文件冻结 M6 丝滑计时边界（任务书 §M6-1 校准目标）。任何阈值调整必须作为显式计划变更（独立 PR + 更新本文件 + 变更说明），禁止在 closeout 阶段临时放宽。",
  "metrics": [
    {
      "id": "json-rpc-bridge",
      "description": "Worker 与 DSH 子进程之间 JSON-RPC bridge 的单次请求/响应往返开销，不含模型推理与设备 I/O",
      "boundary": "request written to stdin → response parsed from stdout，扣除模型生成时间",
      "p95Ms": 100,
      "source": "任务书 M6-1：JSON-RPC bridge p95 ≤100 ms"
    },
    {
      "id": "grounding-decision",
      "description": "Grounding Runtime 对一个已冻结 frame 产生 grounding decision 的服务端开销",
      "boundary": "冻结 frame 输入 → ALLOW_ONCE/REPLAN/HARD_STOP decision 落盘，不含截图采集",
      "p95Ms": 1000,
      "source": "任务书 M6-1：grounding decision p95 ≤1 s"
    },
    {
      "id": "observe-to-dispatch-overhead",
      "description": "稳定 observe 到 action dispatch 之间的非模型开销（frame 稳定化、block 解析、授权复核、ledger 写入）",
      "boundary": "phone_observe 完成 → Control Plane dispatch 开始，扣除模型推理时间",
      "p95Ms": 4000,
      "source": "任务书 M6-1：observe-to-dispatch 非模型开销 p95 ≤4 s"
    }
  ],
  "hardwareProfile": {
    "id": "m6-1-calibration-hermetic",
    "description": "M6-1 离线校准：CI/本机在 hermetic fixture provider 下度量 grounding-decision p95；真机 pinned Windows runner 在 M6-2/M6-3 canary 时单独冻结。M6-1 的 SLO 度量只约束 Grounding Runtime 侧，不含模型/设备 I/O。",
    "runnerOs": "windows",
    "aliases": [
      "01",
      "02",
      "03",
      "04"
    ],
    "calibratedAt": "2026-08-21T00:00:00.000Z",
    "provider": "fixture-provider (hermetic, content-addressed, in-repo)"
  },
  "modelProfile": {
    "id": "m6-1-hermetic-fixture-provider",
    "description": "M6-1 校准 profile：仓内 hermetic fixture provider（零外部模型权重）；M6-3 接入真实 DSH/Cordis 时另立 profile。",
    "locked": true,
    "providerId": "fixture-provider",
    "providerVersion": "1.0.0",
    "groundingDecisionP95Ms": "≤1000 (M6-1 度量 receipt 实测 p95 < 100ms)"
  },
  "temperature": {
    "warm": "DSH 进程与模型已驻留，同一 WorkerRun 内复用；SLO 主要约束 warm 路径",
    "cold": "WorkerRun 首次启动 DSH/加载模型；cold 允许一次性超过 p95，但每个 WorkerRun 只允许发生一次"
  },
  "notes": [
    "一个 WorkerRun 只启动一次 DSH；不得因每个动作重启进程来规避 cold 开销（任务书 §12.10）。",
    "不达标时先优化实现，禁止以增加人工确认换取表面达标（任务书 §M6-1）。"
  ]
}
