# XHS Routine V3 Plan Review Context V1

This is the explicit context packet for the frozen Plan V1. It intentionally excludes credentials, device serials, raw runtime configuration, private goals/queries, screenshots, XML dumps, and unrelated repository contents.

## Original request

V2.1 Closure is reported complete. Continue with V3 “free exploration” for Xiaohongshu, building on the now-proven read-only alias-03 routine and exact read-only `[03,04]` concurrency.

## User-stated V2.1 terminal facts

- Final code/tooling commits on the worktree: `5dab77f` and documentation `a7b7fbb`.
- Reported tests: 216/216 routine, 144/144 pack, 20/20 fusion.
- B4 live waves on `xw-xhs-routine-b4-8aaba01`: S1 note, S1 video, and exact `[03,04]` read-only parallel passed with zero social transport.
- Historical S2/S3 remain immutable transported/ambiguous and are not rerun.
- Aggregate remains partial and `S1_S4_LIVE_VERIFIED=false`.
- Remaining merge blockers are formal Gate F deployment identity and a production vision provider.

## Independently verified repository/runtime facts

- Clean source worktree: `C:/Users/Public/xw-fusion/xw-platform/.worktrees/xhs-r03`, branch `codex/xhs-routine-03-live-s1-s4`, HEAD `a7b7fbbd536522352972b85a5789718dcc7146a0`.
- The root checkout is dirty and not a safe V3 branch base.
- Deployed B4 source is `8aaba01...`, before S_B7 `5dab77f`.
- Current CP/Registry/serve-03/serve-04 report B4 identity; CP health was 200 with zero active leases during inspection.
- The SYSTEM Control Plane task invokes `launch-control-plane.simple.ps1`; formal binding remains on `xw-m6-c1-c7b0695`; current Gate-F status is 503 unavailable; the running CP is not task-owned.
- Current rollback tuple does not hash the runtime-only launcher body.
- Production routine vision config is absent. The code has a real provider wrapper/navigator/factory, but the production runtime does not inject the factory.
- Current provider execution is synchronous/blocking and its declared model hash is not recomputed against installed bytes.

## Existing code facts used by the plan

- `xhs-routine-plan.mjs` has sealed plans, 03-first placement, exact `[03,04]` read-only parallelism, and four fixed templates. `goalSignature` is not in the semantic hash.
- `xhs-feed-routine-machine.mjs` is a fixed feed loop and should not absorb goal-search branching.
- `xhs-routine-runner.mjs` already provides formal sessions, fresh execution IDs, 03->04 acquire order, all-settled child execution, bound artifacts, and cleanup.
- The CP Explorer path supports raw input/tap/swipe/back/screen/dump primitives. A generic session token can therefore express raw coordinates unless a V3-specific profile blocks them.
- Current V2 vision is one-shot and label-oriented. It is not a multi-step DUMP/VISION arbitration layer.
- Current novelty is run-local/coordinate-sensitive and parallel child receipts do not prove shared de-duplication or independent cleanup.
- The codebase contains Standing Grant/DiscoverySession scaffolding, but production discovery remains gated and single-device; enabling it is not required for a zero-effect V3 routine.
- Legacy `nonpayment_v1` semantics may soften some non-payment scope failures. V3 therefore needs an independent hard-zero exploration profile.

## Fixed scope and non-goals

- V3 is read-only and R0 navigation only.
- Fixed roles: alias 03 feed lane; alias 04 search lane; exact pair only.
- No social, protected, account, purchase, or publish/delete actions.
- No alias 01/02, alias 04 single run, fallback, work stealing, bulk scraping, remote vision egress, or open-ended model actions.
- Offline implementation may precede Gate F/provider provisioning; no V3 live acceptance or merge-ready claim may.

## Review question

Does Plan V1 fully and minimally specify a safe, implementable, testable route to bounded goal-driven read-only exploration, especially across these P1 areas?

1. CP trust boundary against raw primitive/social-coordinate bypass.
2. Hard-zero behavior under active `nonpayment_v1`.
3. Atomic shared budgets, stable cross-lane novelty, replay/crash behavior.
4. DUMP-first and local pinned vision with independent oracles.
5. Exact `[03,04]` all-settled cleanup and formal deployment/rollback identity.
6. Public/private evidence separation and reproducible closeout.

Reject scope expansion, style-only feedback, unsupported hypotheticals, and proposals that re-enable social actions or broaden devices.
