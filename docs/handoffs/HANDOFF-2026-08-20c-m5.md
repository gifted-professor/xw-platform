# M5 编排层 — 实施账本（2026-08-20）

> 原始目标与验收真源：`docs/plans/M5-task-brief.md`。本文只记录实施状态和可复核证据；未完成项不写成已完成。

## 当前基线

- M5 feature worktree：`.claude/worktrees/m5-feat`，已 rebase 到 `main@db8885eb00b50ba80677cebd865e4765e3d7929e`。
- 该 main 已含 PR #28、#29、#32、#33、#34。PR #34 已完成 `xw-mission` / `OrchestrationStore` 默认 outbox 到 `xw-runtime` 的收口，M5 不重复改旧路径。
- 正式可复用设备能力是 `xhs.observe.feed`（implemented / automatic / E3 / R0 / effect=none）。`wechat.observe.balance` 仍是 dependency_pending 草案，且视觉脚本未提交，不进入 M5 catalog。
- M5-0 PR #35 的真实 rebase 冲突已在本地解决。Windows CI 风险定位为 test-file 并发下 registry 子进程饥饿 + 启动失败后清理不完整；本地修复使用串行 test-file、20 秒有界启动和稳健终止。
- 当前本地分支 `feat/m5-orchestration-layer` 共 8 笔功能提交（`56374e0`..`6bbd1d9`），仍未 push/merge，也没有部署新 release。

## 已实现（本地提交，尚未 push/merge）

### Router / DAG

- `task-router.mjs`：自然语言或结构化 goal → search / collection / validation / needs_human；确定性、无 LLM、无设备 IO、未知和歧义 fail-closed。
- 任务书原句“四台机器各刷一次首页并汇总卡片数”固定分类为 collection + 4 workers + validatorRequired。
- `dag-compiler.mjs`：生成无环 DAG；严格校验依赖、alias 01..04、完整 SkillVersionRef、参数禁区和本地 validator 终端性。
- planHash 覆盖 catalogHash、完整节点、effect、requiresHuman、参数与依赖；可信 business effect 自动导出 WAIT_HUMAN，调用方不能用旧 `externalEffect` 自报。

### 版本化 Skill 注册真源

- 合同：`packages/kernel/contracts/orchestration/skill-catalog.v1.schema.json`。
- 清单：`services/orchestrator/contracts/m5-skill-catalog.v1.json`。
- 加载器：`services/orchestrator/scripts/lib/skill-catalog.mjs`。
- 每个注册项绑定 SkillSpec、SkillVersionRef、Git sourceCommit/sourceBlobSha、executor 和 effectContract；加载时复核 Git blob、正式 capability lifecycle/automation/effect，以及本地 validator 的实际导出。
- 首批只注册 `xhs.observe.feed`（正式 capability）和 `xw.validate.card-count`（纯本地四 alias/cardCount validator）。

### M5-B 持久 Trace

- `TraceStore` 落到 runtime layout 的 `state/orchestrator/trace`，按 `sha256(traceId).jsonl` 分文件；七类事件、连续 seq、eventId/traceId 绑定和 64 KiB 单事件上限均强校验。
- `queryTrace({traceId})` 为 additive overload，关闭 session 或新建 runtime 后仍可查；旧 `queryTrace({harnessSessionId})` 四字段 shape 不变。
- 既有 TaskPlanV2 scheduler 通过薄 bridge 映射 WorkerAssigned / SkillStarted / SkillFinished / RepairTriggered / ValidationPassed；没有新增调度器。
- Trace 定向与 M4-B 回归 17/17 PASS；scheduler bridge 定向 17/17 PASS。

### M5-C runtime / CLI

- `m5-orchestration-runtime.mjs` 把 catalog → Router → DAG 下沉为既有 TaskPlanV2，再调用 live capability binder 和原 scheduler；没有第三套调度器。
- `xw-task plan-goal --dry-run`、`xw-mission plan-goal --dry-run` 和 registry 的显式 M5 dry-run 路由均为零 state/trace/lease/submit。
- `xw-mission run --goal ... --trace-id ... --execute` 使用统一 `XW_RUNTIME_ROOT`、正式 Control Plane worker 和既有 closeout run；本地 `xw.validate.card-count` validator 真正决定最终状态。
- fake E2E 已覆盖四 alias 全成功、validator 失败以及 alias 03 失败而其余三路成功的隔离语义。
- live 接线期间修正了两处只在正式环境暴露的漂移：设备 eligibility 改读现行 `implementationSupport.job`，Registry/Control Plane capability integrity 改用 CP v2 算法且漂移时 fail-closed。

### 当前测试证据

- Router + DAG + catalog 定向测试：49/49 PASS。
- 覆盖非法/重复 skillId、SkillVersionRef 不匹配、伪造 effect、伪造 source blob、循环/自依赖/未知依赖、非法 alias、needs_human、真实四机目标编译。
- `npm run check`、`fusion:verify`、authority、kernel、M4-A/B/C/D、M0、runtime tools、`test:fusion` 全绿。
- 直接 `npm run test:orchestrator`：407/407 PASS；不再依赖 orchestrator known-failure allowlist。
- `test:gate` 的 orchestrator 为 PASS；control-plane 的 28 个既有失败均精确命中既有 allowlist，无 unexpected failure。
- `multi-model-review-gate` 首轮结论为 FIX_REQUIRED；所报 runtime eligibility 与 capability integrity 问题已分别由 `1b1c124`、`6bbd1d9` 修复。限定范围的 `VERIFY_FIXES` 复核为 PASS：相关 12/12、`npm run check`、`fusion:verify` 均通过。

## 真机验收账本

### 成功场景：通过

- actor：`claude-pilot-20260809`；目标：“四台机器各刷一次首页并汇总卡片数”。这里的“刷一次”按任务书约束解释为已有只读 `xhs.observe.feed`，不新增 swipe/refresh 能力。
- closeout run：`run_abca2ad6-2bf1-4cb2-9f50-6c5786e55214`。
- traceId：`trace-m5-live-success-20260820d`；DAG hash：`817a3033d197bd3bc6ca7af69e7faea24f9a00e78922c3fdf10dba414f2bda6a`；TaskPlan hash：`ddbe9a4d2852ef765d8e865268f298c5ccab59ec7addb3694e731d479bf85699`；ExecutionPlan hash：`c537d568326e4bfb0065f84d3da464e0d220ef4b777bee294705b735881d772a`。
- 4/4 accepted：01=`job_cac65a23-4b04-4164-8d57-cc6ed2d8c6de`、02=`job_8f649709-03f9-47db-90ca-daccb48a401b`、03=`job_5eb358dc-066e-42bd-aad0-f84198c5cb21`、04=`job_3b0beb43-da85-490c-bb8a-e42a87eef45f`。
- cardCount：01=2、02=2、03=2、04=2，总数 8；本地 validator 通过后才写 `ValidationPassed`。
- 新建 Harness runtime 后调用 `queryTrace({traceId})` 得到 seq 1..17 的完整事件链；integrity SHA-256=`0e477a98bea2bd0941113252c2a2ba24782d659e310188a93e22348ec3d1181f`。

### alias 03 停服隔离演练：未取得有效失败证据

- 第一次 run `run_df33f367-9a14-4c6a-9f9d-9a691973b700` / trace `trace-m5-live-failure-20260820` 在演练前置发生 App focus 漂移，四路均为 `xhs.unknown`，不能证明单机隔离。
- 完成正式 readiness 恢复与四机 focus 证明后，第二次 run `run_8014d28c-9f0d-4fd3-b55e-557532ef6f5a` / trace `trace-m5-live-failure-20260820b` 使用正式 scheduled-task Stop/Start 脚本；但四个只读 job 太快，03 在停服真正生效前已经成功，结果仍为 4/4 accepted、总 cardCount=8。
- 第二次 trace 仍完整可查：17 events、seq 1..17、integrity SHA-256=`5714cc8166d107b69f95d293ba53effdb72a69f236b998dcd57475f14136050e`，且它正确记录 03 `SkillFinished(succeeded)` 和最终 `ValidationPassed`；因此不得把本次注入写成隔离通过。
- 另有运维发现：Stop 脚本对当前 listener command line 的保护性匹配会先报“unrelated listener”，scheduled task 停止随后才使 17898 不监听。这会放大短任务的注入竞态；本轮没有用 `Stop-Process` 或其他旁路。
- fake E2E 的单节点失败隔离语义已通过，但任务书要求的真机 kill-one-serve 项仍是唯一未完成验收项。后续应先设计可确定命中的受管 failpoint/较长只读 fixture，再单独批准重跑；不要靠反复人工抢时序。

### 演练后恢复证明

- alias 03 正式服务已恢复：task state=`Running`、17898 listening=true。
- `xw-start --check --json`：ready=true、ADB 5038 四机三次采样均 stable，四个 serve 均 listening，四机均 ready。
- activeLeases=0、runningJobs=0、mutationCount=0、blockerCount=0。
- runtime release 保持 `xw-20260819-f337079`，sourceCommit=`f337079d93b6e16993b93f7d28783f57da9a5184`；`MULTI_AGENT_LIVE` / `DSH_LIVE` / `OPEN_ACTION_LIVE` 仍 CLOSED。

## 下一步顺序

1. 对当前本地提交做最终 diff review；如无新问题，再按项目规则创建 PR（仍用 merge commit，不 squash）。
2. 真机成功场景已经完成；不要重复碰设备。
3. alias 03 kill-one-serve 验收仍未完成。先单独修/设计受管故障注入时序并获得重跑授权，再补这一个证据；当前分支不能宣称 M5 全部验收完成。

## 不变量

- 不新增设备能力，不部署新 release，不直写 control.db/registry.db，不绕 lease，不碰支付。
- 不新建第三套 scheduler：M5 DAG 通过 adapter 下沉到既有 TaskPlanV2/ExecutionPlanV2 调度器。
- `MULTI_AGENT_LIVE` / `DSH_LIVE` / `OPEN_ACTION_LIVE` 继续 CLOSED。
- 合并只用 merge commit；导入服务树的每个新增/修改路径精确登记 post-import allowlist。

