# M5 编排层 — 实施账本（2026-08-20）

> 原始目标与验收真源：`docs/plans/M5-task-brief.md`。本文只记录实施状态和可复核证据；未完成项不写成已完成。

## 当前基线

- M5 feature worktree：`.claude/worktrees/m5-feat`，已 rebase 到 `main@db8885eb00b50ba80677cebd865e4765e3d7929e`。
- 该 main 已含 PR #28、#29、#32、#33、#34。PR #34 已完成 `xw-mission` / `OrchestrationStore` 默认 outbox 到 `xw-runtime` 的收口，M5 不重复改旧路径。
- 正式可复用设备能力是 `xhs.observe.feed`（implemented / automatic / E3 / R0 / effect=none）。`wechat.observe.balance` 仍是 dependency_pending 草案，且视觉脚本未提交，不进入 M5 catalog。
- M5-0 PR #35 的真实 rebase 冲突已在本地解决。Windows CI 风险定位为 test-file 并发下 registry 子进程饥饿 + 启动失败后清理不完整；本地修复使用串行 test-file、20 秒有界启动和稳健终止。
- 分支已重放到 M5-0 之后的 main（`315ad7c`）：7 笔功能提交 patch-id 逐笔全等，另有收口账本/计划提交；仍待 M5 功能 PR 合入，没有部署新 release。

## 已实现（本地提交，尚未 push/merge）

### Router / DAG

- `task-router.mjs`：自然语言或结构化 goal → search / collection / validation / needs_human；确定性、无 LLM、无设备 IO、未知和歧义 fail-closed。
- 任务书原句“四台机器各刷一次首页并汇总卡片数”固定分类为 collection + 4 workers + validatorRequired。
- `dag-compiler.mjs`：生成无环 DAG；严格校验依赖、alias 01..04、完整 SkillVersionRef、参数禁区和本地 validator 终端性。
- planHash 覆盖 catalogHash、完整节点、effect、requiresHuman、参数与依赖；可信 business effect 自动导出 WAIT_HUMAN，调用方不能用旧 `externalEffect` 自报。

### 版本化 Skill 注册真源

- 合同：`packages/kernel/contracts/orchestration/skill-catalog.v1.schema.json`。
- 清单：`services/orchestrator/contracts/m5-skill-catalog.v1.json`。
- 加载器：`services/orchestrator/scripts/lib/skill-catalog.mjs`。
- 每个注册项绑定 SkillSpec、SkillVersionRef、Git sourceCommit/sourceBlobSha、executor 和 effectContract；加载时复核 Git blob、正式 capability lifecycle/automation/effect，以及本地 validator 的实际导出。
- 首批只注册 `xhs.observe.feed`（正式 capability）和 `xw.validate.card-count`（纯本地四 alias/cardCount validator）。

### M5-B 持久 Trace

- `TraceStore` 落到 runtime layout 的 `state/orchestrator/trace`，按 `sha256(traceId).jsonl` 分文件；七类事件、连续 seq、eventId/traceId 绑定和 64 KiB 单事件上限均强校验。
- `queryTrace({traceId})` 为 additive overload，关闭 session 或新建 runtime 后仍可查；旧 `queryTrace({harnessSessionId})` 四字段 shape 不变。
- 既有 TaskPlanV2 scheduler 通过薄 bridge 映射 WorkerAssigned / SkillStarted / SkillFinished / RepairTriggered / ValidationPassed；没有新增调度器。
- Trace 定向与 M4-B 回归 17/17 PASS；scheduler bridge 定向 17/17 PASS。

### M5-C runtime / CLI

- `m5-orchestration-runtime.mjs` 把 catalog → Router → DAG 下沉为既有 TaskPlanV2，再调用 live capability binder 和原 scheduler；没有第三套调度器。
- `xw-task plan-goal --dry-run`、`xw-mission plan-goal --dry-run` 和 registry 的显式 M5 dry-run 路由均为零 state/trace/lease/submit。
- `xw-mission run --goal ... --trace-id ... --execute` 使用统一 `XW_RUNTIME_ROOT`、正式 Control Plane worker 和既有 closeout run；本地 `xw.validate.card-count` validator 真正决定最终状态。
- fake E2E 已覆盖四 alias 全成功、validator 失败以及 alias 03 失败而其余三路成功的隔离语义。
- live 接线期间修正了两处只在正式环境暴露的漂移：设备 eligibility 改读现行 `implementationSupport.job`，Registry/Control Plane capability integrity 改用 CP v2 算法且漂移时 fail-closed。

### 当前测试证据

- Router + DAG + catalog 定向测试：49/49 PASS。
- 覆盖非法/重复 skillId、SkillVersionRef 不匹配、伪造 effect、伪造 source blob、循环/自依赖/未知依赖、非法 alias、needs_human、真实四机目标编译。
- `npm run check`、`fusion:verify`、authority、kernel、M4-A/B/C/D、M0、runtime tools、`test:fusion` 全绿。
- 直接 `npm run test:orchestrator`：407/407 PASS；不再依赖 orchestrator known-failure allowlist。
- `test:gate` 的 orchestrator 为 PASS；control-plane 的 28 个既有失败均精确命中既有 allowlist，无 unexpected failure。
- `multi-model-review-gate` 首轮结论为 FIX_REQUIRED；所报 runtime eligibility 与 capability integrity 问题已分别由 `1b1c124`、`6bbd1d9` 修复。限定范围的 `VERIFY_FIXES` 复核为 PASS：相关 12/12、`npm run check`、`fusion:verify` 均通过。

## 真机验收账本

### 成功场景：通过

- actor：`claude-pilot-20260809`；目标：“四台机器各刷一次首页并汇总卡片数”。这里的“刷一次”按任务书约束解释为已有只读 `xhs.observe.feed`，不新增 swipe/refresh 能力。
- closeout run：`run_abca2ad6-2bf1-4cb2-9f50-6c5786e55214`。
- traceId：`trace-m5-live-success-20260820d`；DAG hash：`817a3033d197bd3bc6ca7af69e7faea24f9a00e78922c3fdf10dba414f2bda6a`；TaskPlan hash：`ddbe9a4d2852ef765d8e865268f298c5ccab59ec7addb3694e731d479bf85699`；ExecutionPlan hash：`c537d568326e4bfb0065f84d3da464e0d220ef4b777bee294705b735881d772a`。
- 4/4 accepted：01=`job_cac65a23-4b04-4164-8d57-cc6ed2d8c6de`、02=`job_8f649709-03f9-47db-90ca-daccb48a401b`、03=`job_5eb358dc-066e-42bd-aad0-f84198c5cb21`、04=`job_3b0beb43-da85-490c-bb8a-e42a87eef45f`。
- cardCount：01=2、02=2、03=2、04=2，总数 8；本地 validator 通过后才写 `ValidationPassed`。
- 新建 Harness runtime 后调用 `queryTrace({traceId})` 得到 seq 1..17 的完整事件链；integrity SHA-256=`0e477a98bea2bd0941113252c2a2ba24782d659e310188a93e22348ec3d1181f`。

### alias 03 停服隔离演练：前两次无效（前置漂移 / 退役脚本）

- 第一次 run `run_df33f367-9a14-4c6a-9f9d-9a691973b700` / trace `trace-m5-live-failure-20260820` 在演练前置发生 App focus 漂移，四路均为 `xhs.unknown`，不能证明单机隔离。
- 完成正式 readiness 恢复与四机 focus 证明后，第二次 run `run_8014d28c-9f0d-4fd3-b55e-557532ef6f5a` / trace `trace-m5-live-failure-20260820b` 使用了任务书明确禁止的退役脚本 `services/control-plane/scripts/fast-operator-serve-task.ps1`（并非正式脚本，该注入证据无效）；且四个只读 job 太快，03 在停服真正生效前已经成功，结果仍为 4/4 accepted、总 cardCount=8。
- 第二次 trace 仍完整可查：17 events、seq 1..17、integrity SHA-256=`5714cc8166d107b69f95d293ba53effdb72a69f236b998dcd57475f14136050e`，且它正确记录 03 `SkillFinished(succeeded)` 和最终 `ValidationPassed`；因此不得把本次注入写成隔离通过。
- 前两次演练均使用了任务书明确禁止的退役脚本 `services/control-plane/scripts/fast-operator-serve-task.ps1`，两轮证据一律无效，不得作为任何验收依据。
- 另有运维发现：该退役 Stop 脚本对当前 listener command line 的保护性匹配会先报“unrelated listener”，scheduled task 停止随后才使 17898 不监听。这会放大短任务的注入竞态；本轮没有用 `Stop-Process` 或其他旁路。
- fake E2E 的单节点失败隔离语义早已通过；真机 kill-one-serve 项由下方第三次有效演练补齐（2026-08-20 收口）。

### alias 03 停服隔离演练：第三次有效通过（2026-08-20 closeout）

- closeout run：`run_1d21ea2c-a3fc-494c-8883-bab6e39cb236`（status=completed，bundle manifestSha256=`eb24a9334d78defb6d36665cd83aefd36b1a50c26c6988ab439aa0ff72d61800`）；traceId：`trace-m5-live-failure-20260820c`；DAG/TaskPlan/ExecutionPlan hash 与成功场景一致。
- runner：一次性 acceptance runner，SHA-256=`95108d137c76cf297489ab90193617d6d6b2c3fb9b4c7372705356a867038e08`，源码已在本文件下方冻结；只组合正式模块，仅包装 `worker.execute(assignment)`，在 03 的 `WorkerAssigned`/`SkillStarted` 持久化之后、正式 job submit 之前 `Stop-ScheduledTask 'XW Platform FastOperator 03'` 并有界确认 17898 关闭；未调退役脚本、未 `Stop-Process`/`taskkill`、未直写 DB。
- 预检：`xw-start --check` ready、四 serve listening、ADB 5038 稳定、activeLeases=0/runningJobs=0；17898 listener PID=23652，command line 绑定当前 release `xw-20260819-f337079` 的 alias 03（serial 211d0120），parent chain 到 Schedule 服务；四机 focus 经正式 Explorer session/lease 两轮证明为 `com.xingin.xhs.index.v2.IndexActivityV2`，释放最后 lease → 派发 handoff=0.02ms（预算 2 秒）。证据：`evidence/precheck-*.json|txt`、`evidence/focus-prep.json`。
- 注入时序：15:41:59.647Z Stop-ScheduledTask → 15:42:01.887Z 确认 17898 不再监听 → 15:42:01.906Z 03 正式 job `job_a79e752d-f8b1-43bb-986c-651074a032ba` bound（端口关闭确认之后）→ job 以 `ADAPTER_HTTP_UNAVAILABLE` 正式失败，保留 job/attempt/error。
- 结果：01/02/04 各自 accepted、cardCount=2/2/2，互不影响；03 `technicalStatus=failed`、`accepted=false`；mission status=`partial`。
- trace：15 events、seq 1..15 连续，integrity SHA-256=`471d222dbe2f5b907a4d5dd6a142695fb8e95a7adf24ecb2f4881100d733cfcf`；03 有 `SkillFinished(failed)` 与 `RepairTriggered`；全链无 `ValidationPassed`（符合“只有 validation.ok===true 才写”的语义）。
- 恢复：runner finally 15:42:06.091Z `Start-ScheduledTask` → 15:42:11.742Z 17898 恢复监听；03 残留 `unresolvedFailure=ADAPTER_HTTP_UNAVAILABLE` 经正式 `xw-start 03` recovery（一单正式 R0 readiness job）清除；最终四机 ready/free、activeLeases=0、runningJobs=0、pendingApprovals=0、release 不变。证据：`evidence/recovery-03.json`、`evidence/post-drill-xw-start.json`、`evidence/post-drill-agent-entry.md`。
- jobIds：01=`job_05d68b77-f971-43fc-993f-a392a045164e`、02=`job_f42b284e-a45a-4ed4-91ad-246942cc15ae`、03=`job_a79e752d-f8b1-43bb-986c-651074a032ba`（failed）、04=`job_c75a8817-3eda-4b2d-b31c-566dd656bc72`。

### 演练后恢复证明

- alias 03 正式服务已恢复：task state=`Running`、17898 listening=true。
- `xw-start --check --json`：ready=true、ADB 5038 四机三次采样均 stable，四个 serve 均 listening，四机均 ready。
- activeLeases=0、runningJobs=0、mutationCount=0、blockerCount=0。
- runtime release 保持 `xw-20260819-f337079`，sourceCommit=`f337079d93b6e16993b93f7d28783f57da9a5184`；`MULTI_AGENT_LIVE` / `DSH_LIVE` / `OPEN_ACTION_LIVE` 仍 CLOSED。

## 收口状态（2026-08-20 终）

1. M5-0 PR #35 已以 merge commit 合入 main（merge SHA=`315ad7cf3af1a64fb4259429d247166e71ef3be4`），main CI 绿；M5-0 可独立回滚。
2. M5 功能分支已 rebase 到 post-35 main：7 笔功能提交 patch-id 与冻结清单逐笔全等（range-diff 全 `=`），外加收口账本/计划提交；真机成功 4/4 与 alias 03 有效隔离 1 fail + 3 accepted 两份证据齐全。
3. 剩余：M5 功能 PR 以 merge commit 合入 main（见 `docs/plans/M5-finalization-plan.md` F5/F6）。

## 不变量

- 不新增设备能力，不部署新 release，不直写 control.db/registry.db，不绕 lease，不碰支付。
- 不新建第三套 scheduler：M5 DAG 通过 adapter 下沉到既有 TaskPlanV2/ExecutionPlanV2 调度器。
- `MULTI_AGENT_LIVE` / `DSH_LIVE` / `OPEN_ACTION_LIVE` 继续 CLOSED。
- 合并只用 merge commit；导入服务树的每个新增/修改路径精确登记 post-import allowlist。



## F3 alias 03 隔离演练 runner：执行前源码冻结

- runner 执行副本 SHA-256：`95108d137c76cf297489ab90193617d6d6b2c3fb9b4c7372705356a867038e08`（执行时必须逐字节一致）。
- 仓库规范副本：`docs/plans/m5-closeout/alias03-isolation-runner.mjs`；执行副本留在 runtime evidence 目录，不进 release。
- 注入点：`task-orchestrator.mjs` 的 `traceBridge.skillStarted(...)` → `worker.execute(assignment)` 相邻行间，仅包装 alias 03；其余 alias 直达原 worker。
- 边界：stop 后 15s/100ms 轮询确认 17898 关闭；mission 120s 封顶；finally 内 Start-ScheduledTask 恢复，30s 确认监听；不调退役 wrapper、不 Stop-Process、不 taskkill、不直写 DB。
- 修订记录：首版 SHA `1e67b2d3…` 在派发前 setup 阶段失败（registry catalog 未取 `.capabilities`，未注入故障、未派发），修正为本 SHA 后重新绑定。

完整源码（与上述 SHA 绑定）：

```javascript
#!/usr/bin/env node
// M5 closeout: one-shot alias 03 serve-stop isolation acceptance runner.
// Composes only existing public modules; does not modify the repo or device capabilities.
// Fault model: wraps worker.execute(assignment). When the first alias "03" assignment
// arrives, its WorkerAssigned/SkillStarted trace events are already persisted by
// task-orchestrator.mjs (traceBridge.skillStarted(...) -> worker.execute(...) on adjacent
// lines). The wrapper stops scheduled task 'XW Platform FastOperator 03' and confirms
// port 17898 is no longer listening BEFORE delegating to the real worker, so the real
// worker still submits the formal job against a stopped serve. The serve stays stopped
// for the whole mission (retries re-assert the stop). finally{} always restores the task.
//
// Usage:
//   node alias03-isolation-runner.mjs --run <closeout-run-id> --trace-id <id> --actor <actor>
//       [--goal <text>] [--aliases 01,02,03,04] [--evidence-dir <dir>]

import { writeFileSync, mkdirSync } from "node:fs";
import { execFile } from "node:child_process";
import { join, resolve } from "node:path";
import { pathToFileURL } from "node:url";
import { promisify } from "node:util";

// This file is executed from a runtime evidence directory, so all repository
// modules are imported dynamically against an explicit repo root.
const REPO_ROOT = resolve(process.env.XW_REPO_ROOT || required(process.argv.slice(2), "--repo-root"));
async function repoModule(rel) {
  return import(pathToFileURL(join(REPO_ROOT, rel)).href);
}
const { OrchestrationStore } = await repoModule("services/orchestrator/scripts/lib/orchestration-store.mjs");
const { ControlPlaneHttpClient, TypedJobWorker } = await repoModule("services/orchestrator/scripts/lib/typed-job-worker.mjs");
const { MissionWorkerRouter, SessionWorkflowWorker } = await repoModule("services/orchestrator/scripts/lib/session-workflow-worker.mjs");
const { executeM5Goal } = await repoModule("services/orchestrator/scripts/lib/m5-orchestration-runtime.mjs");
const { loadLiveFleet, reconcileLiveCapabilityCatalog } = await repoModule("services/orchestrator/ops/xw-mission.mjs");
const { TraceStore } = await repoModule("packages/harness-protocol/lib/trace-store.mjs");
const { acquireExplorerSession, releaseExplorerSession } = await repoModule("services/orchestrator/ops/_explore-lease.mjs");
const { executeExplorerSessionAction } = await repoModule("services/orchestrator/ops/_explore-session-action.mjs");

const execFileAsync = promisify(execFile);
const RUNTIME_ROOT = resolve(process.env.XW_RUNTIME_ROOT || "C:\\Users\\Public\\xw-runtime");
const WORK_ROOT = join(RUNTIME_ROOT, "state", "orchestrator", "outbox", "work");
const TASK_NAME = "XW Platform FastOperator 03";
const PORT_03 = 17898;
const STOP_CONFIRM_BUDGET_MS = 15000;
const PORT_POLL_MS = 100;
const MISSION_BUDGET_MS = 120000;
const RESTORE_CONFIRM_BUDGET_MS = 30000;
const FOCUS_HANDOFF_BUDGET_MS = 2000;
const XHS_FOCUS = { package: "com.xingin.xhs", activity: /index\.v2\.IndexActivityV2/ };

function option(argv, name, fallback = undefined) {
  const index = argv.indexOf(name);
  return index >= 0 ? argv[index + 1] : fallback;
}
function required(argv, name) {
  const value = option(argv, name);
  if (!value) throw new Error(`${name} is required`);
  return value;
}
const mono = () => Number(process.hrtime.bigint()) / 1e6; // monotonic ms

async function ps(command) {
  const { stdout } = await execFileAsync("powershell.exe", ["-NoProfile", "-NonInteractive", "-Command", command], { timeout: 30000 });
  return String(stdout).trim();
}

async function portListening() {
  const out = await ps(`if (Get-NetTCPConnection -LocalPort ${PORT_03} -State Listen -ErrorAction SilentlyContinue) { 'yes' } else { 'no' }`);
  return out === "yes";
}

async function stopServe03(evidence) {
  await ps(`Stop-ScheduledTask -TaskName '${TASK_NAME}'`);
  evidence.stopIssuedAt = { mono: mono(), iso: new Date().toISOString() };
  const deadline = mono() + STOP_CONFIRM_BUDGET_MS;
  while (mono() < deadline) {
    if (!(await portListening())) {
      evidence.portClosedAt = { mono: mono(), iso: new Date().toISOString() };
      return;
    }
    await new Promise((r) => setTimeout(r, PORT_POLL_MS));
  }
  throw new Error(`port ${PORT_03} still listening ${STOP_CONFIRM_BUDGET_MS}ms after Stop-ScheduledTask`);
}

async function restoreServe03(evidence) {
  await ps(`Start-ScheduledTask -TaskName '${TASK_NAME}'`);
  evidence.startIssuedAt = { mono: mono(), iso: new Date().toISOString() };
  const deadline = mono() + RESTORE_CONFIRM_BUDGET_MS;
  while (mono() < deadline) {
    if (await portListening()) {
      evidence.portRestoredAt = { mono: mono(), iso: new Date().toISOString() };
      return true;
    }
    await new Promise((r) => setTimeout(r, PORT_POLL_MS));
  }
  return false;
}

// Prove one alias' XHS foreground via a formal Explorer session/lease; the session
// is always released in finally. Returns captured focus evidence.
async function proveAliasFocus(alias, actorId) {
  const proof = { alias };
  const { path: contextPath } = await acquireExplorerSession({ alias, actor: actorId });
  try {
    const result = await executeExplorerSessionAction({ contextPath, params: { primitive: "focus" } });
    proof.focus = { package: result.output?.package ?? null, activity: result.output?.activity ?? null };
    proof.readAt = { mono: mono(), iso: new Date().toISOString() };
    if (proof.focus.package !== XHS_FOCUS.package || !XHS_FOCUS.activity.test(String(proof.focus.activity || ""))) {
      throw Object.assign(new Error(`alias ${alias} focus is not XHS index: ${JSON.stringify(proof.focus)}`), { code: "M5_DRILL_FOCUS_MISMATCH" });
    }
    return proof;
  } finally {
    await releaseExplorerSession({ contextPath }).catch((error) => {
      proof.releaseError = error?.message || String(error);
    });
    proof.releasedAt = { mono: mono(), iso: new Date().toISOString() };
  }
}

async function main() {
  const argv = process.argv.slice(2);
  const taskRunId = required(argv, "--run");
  const traceId = required(argv, "--trace-id");
  const actorId = required(argv, "--actor");
  const goal = option(argv, "--goal", "四台机器各刷一次首页并汇总卡片数");
  const aliases = String(option(argv, "--aliases", "01,02,03,04")).split(/[,:\s]+/).filter(Boolean);
  const evidenceDir = required(argv, "--evidence-dir");
  mkdirSync(evidenceDir, { recursive: true });

  const registryUrl = "http://127.0.0.1:17930/";
  const client = new ControlPlaneHttpClient({ baseUrl: "http://127.0.0.1:17920/" });
  const typedJobWorker = new TypedJobWorker({ client, actorId, pollMs: 1000 });
  const sessionWorkflowWorker = new SessionWorkflowWorker({ client, actorId, pollMs: 0 });
  const router = new MissionWorkerRouter({ typedJobWorker, sessionWorkflowWorker });

  const evidence = { taskName: TASK_NAME, port: PORT_03, alias03Attempts: 0, injections: [] };

  const wrappedWorker = {
    async execute(assignment) {
      if (assignment?.alias === "03") {
        evidence.alias03Attempts += 1;
        const injection = { attemptId: assignment.attemptId, attemptIndex: assignment.attemptIndex };
        evidence.injections.push(injection);
        // Keep 03 serve stopped for the whole mission, including safe retries.
        if (await portListening()) await stopServe03(evidence);
        injection.delegatingAt = { mono: mono(), iso: new Date().toISOString() };
      }
      return router.execute(assignment);
    },
  };

  const store = new OrchestrationStore({ taskRunId, workRoot: WORK_ROOT });
  const [registryCatalog, controlCatalogResponse] = await Promise.all([
    (async () => {
      const response = await fetch(new URL("api/capabilities", registryUrl), { signal: AbortSignal.timeout(10000) });
      const result = await response.json();
      if (!response.ok || result?.ok === false) throw new Error(result?.error?.message || `registry catalog failed ${response.status}`);
      return result.capabilities || [];
    })(),
    client.getCapabilities(),
  ]);
  const liveCatalog = reconcileLiveCapabilityCatalog(
    registryCatalog,
    controlCatalogResponse?.capabilities || controlCatalogResponse?.data?.capabilities || [],
  );

  let missionResult = null;
  let missionError = null;
  let restored = false;

  // F3 precheck 6.1.3: formal Explorer-session focus proof on every alias, sessions
  // released in finally; then a fresh formal re-read right before dispatch. If any
  // alias focus mismatches, or the release->dispatch handoff exceeds 2s, abort
  // BEFORE executeM5Goal so no fault is ever injected.
  try {
    evidence.focusProofRound1 = [];
    for (const alias of aliases) evidence.focusProofRound1.push(await proveAliasFocus(alias, actorId));
    evidence.focusProofRound2 = [];
    for (const alias of aliases) evidence.focusProofRound2.push(await proveAliasFocus(alias, actorId));
    const lastReleaseMono = evidence.focusProofRound2[evidence.focusProofRound2.length - 1].releasedAt.mono;
    evidence.dispatchHandoffMs = mono() - lastReleaseMono;
    if (evidence.dispatchHandoffMs > FOCUS_HANDOFF_BUDGET_MS) {
      throw Object.assign(new Error(`focus handoff ${evidence.dispatchHandoffMs.toFixed(1)}ms exceeds ${FOCUS_HANDOFF_BUDGET_MS}ms budget`), { code: "M5_DRILL_HANDOFF_EXCEEDED" });
    }
  } catch (error) {
    // Abort before dispatch: no fault was injected; verify 03 serve is still up as a safeguard.
    restored = await portListening().catch(() => false);
    if (!restored) restored = await restoreServe03(evidence).catch(() => false);
    evidence.abortedBeforeDispatch = true;
    const output = { ok: false, restored, evidence, missionError: { code: error?.code || "M5_DRILL_ABORTED", message: error?.message || String(error) }, result: null };
    writeFileSync(join(evidenceDir, "alias03-isolation-result.json"), JSON.stringify(output, null, 2));
    console.log(JSON.stringify(output, null, 2));
    process.exit(4);
  }

  try {
    const mission = executeM5Goal({
      goal,
      aliases,
      traceId,
      taskRunId,
      liveCatalog,
      fleetProvider: () => loadLiveFleet({ registryUrl }),
      worker: wrappedWorker,
      store,
      traceStore: new TraceStore(),
    });
    const timeout = new Promise((_, reject) =>
      setTimeout(() => reject(Object.assign(new Error("mission budget exceeded"), { code: "M5_DRILL_TIMEOUT" })), MISSION_BUDGET_MS),
    );
    missionResult = await Promise.race([mission, timeout]);
  } catch (error) {
    missionError = { code: error?.code || "M5_DRILL_FAILED", message: error?.message || String(error) };
  } finally {
    restored = await restoreServe03(evidence);
  }

  const output = { ok: !missionError, restored, evidence, missionError, result: missionResult };
  writeFileSync(join(evidenceDir, "alias03-isolation-result.json"), JSON.stringify(output, null, 2));
  console.log(JSON.stringify(output, null, 2));
  if (!restored) {
    console.error("WARNING: alias 03 serve did not restore within budget; run formal xw-start recovery");
    process.exit(3);
  }
  if (missionError) process.exit(2);
}

main().catch((error) => {
  console.error(JSON.stringify({ ok: false, error: { code: error?.code || "M5_DRILL_RUNNER_FAILED", message: error?.message || String(error) } }, null, 2));
  process.exit(1);
});
```
